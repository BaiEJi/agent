"""
全局常量与应用设置模块

存放不随环境变化的固定配置（如 API 路径前缀、分页大小等）。
与 env.py 的区别：
- env.py: 环境相关，从 .env 读取，不同环境不同值
- settings.py: 固定常量，所有环境一致
"""

# ============================================================
# API 路由前缀
# 所有接口统一挂在 /api/v1 下，便于版本管理
# ============================================================
API_V1_PREFIX = "/api/v1"

# ============================================================
# Agent 相关常量
# ============================================================

# Agent 单轮对话最大 token 数
AGENT_MAX_TOKENS = 4096

# Agent 上下文窗口保留的对话轮数
AGENT_CONTEXT_WINDOW = 20

# ============================================================
# 记忆相关常量
# ============================================================

# 短期记忆在 Redis 中的过期时间（秒）
# 默认 24 小时，过期后自动清理
SHORT_TERM_MEMORY_TTL = 86400

# 长期记忆摘要触发阈值：对话轮数超过此值时触发摘要压缩
LONG_TERM_MEMORY_SUMMARY_THRESHOLD = 50

# ============================================================
# 会话相关常量
# ============================================================

# 会话 ID 的长度（字符数）
SESSION_ID_LENGTH = 32

# 会话过期时间（秒），默认 7 天
SESSION_TTL = 604800

# ============================================================
# 分页默认值
# ============================================================
DEFAULT_PAGE_SIZE = 20
MAX_PAGE_SIZE = 100
